apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
        - name: react
          image: andremugabo/frontend:latest  # Replace with your registry
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: react-app-config